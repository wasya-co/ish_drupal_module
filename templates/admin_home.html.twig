
{# <h1>Administration</h1> #}

<div class="actions">
  {% for link in secondary_menu_links %}
    <a class="button button--primary"
       href="{{ link.url }}"
       {% if link.confirm %}onclick="return confirm('Are you sure?');"{% endif %}
    >{{ link.title }}</a>
  {% endfor %}
</div>

<hr>

{# <h2>Maintenance</h2> #}

{# <p>Put anything you want here.</p> #}

{# <ul>
  <li>Task status</li>
  <li>Statistics</li>
  <li>Forms</li>
  <li>Tables</li>
  <li>Logs</li>
</ul> #}
